# R 코드에서의 주석(comment, 프로그램 설명) - 실행되지 않는 코드.
# R의 주석은 #으로 시작해서 그 라인(줄)의 끝까지.
# R Studio에서 코드 실행: 
#   Ctrl+Enter: 커서가 있는 위치의 한 문장을 실행

x <- 10  # 변수(객체) x에 값(value) 1을 할당.
x  # 변수 x에 저장된 값을 출력.

y <- 15
y
